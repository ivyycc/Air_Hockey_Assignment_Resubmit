struct BoundaryHolder
{
    public float top, bottom, left, right;
    public BoundaryHolder(float t, float b, float l, float r)
    {
        top = t;
        bottom = b;
        left = l;
        right = r;

    }
}

